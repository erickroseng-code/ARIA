# Story 2.4: Plano de Ataque — Page Creation in Notion

## Status
Done

## Executor Assignment
```yaml
executor: "@dev"
quality_gate: "@architect"
quality_gate_tools: ["architecture-review", "typecheck", "lint", "vitest"]
```

## Story

**As a** usuário,
**I want** que a ARIA crie automaticamente uma página "Plano de Ataque" estruturada no Notion após confirmar o preview da análise,
**so that** eu tenha o plano organizado, formatado e pronto para apresentação ao cliente em menos de 3 minutos — sem intervenção manual.

## Acceptance Criteria

1. Página criada com título `🎯 Plano de Ataque — [DD/MM/YYYY]` dentro do perfil do cliente no Notion
2. Blocos Notion usados: `heading_1`, `heading_2`, `bulleted_list_item`, `to_do`, `callout`, `divider`
3. Seções em ordem obrigatória: Contexto Geral → Resumo por Setor → Análise Integrada → Checklists de Ações
4. Checklists criados como blocos `to_do` nativos com `checked: false`
5. Callouts para insights críticos (`>` do Markdown) com emoji relevante (💡, ⚠️, 🎯)
6. Link direto para a página enviado ao usuário após criação
7. Confirmação: `"✅ Plano de Ataque criado no Notion para *Empresa X* — [link]"`
8. Trigger: usuário digita "confirmar" (ou variações) após ver preview da Story 2.3
9. Página criada como **subpágina** dentro da página do cliente (parent = `notionPageId` do cliente)
10. KPI MVP: criação concluída em **menos de 30 segundos** após confirmação (NFR1 adaptado)

## Complexity Estimate

**T-Shirt Sizing:** Medium (M)
**Story Points:** 8-10 points
**Estimated Dev Time:** 2-3 days
**Estimated QA Time:** 1 day

**Complexity Drivers:**
- Markdown to Notion block conversion
- Nested block hierarchy (headings → items)
- Notion API page creation

## Risks & Mitigation

| Risk | Impact | Likelihood | Mitigation |
|------|--------|-----------|-----------|
| **Notion block hierarchy incorrect** | Page structure broken | Medium | Test with sample markdown; validate nesting |
| **API call timeout** | Page creation fails | Low | Implement timeout of 10s |
| **To-do blocks not created** | Checklists missing | Low | Test markdown parsing with 20+ examples |
| **Parent page ID invalid** | Subpage creation fails** | Low | Validate pageId exists before create |

## Out of Scope

- ❌ **Custom formatting** — Markdown only
- ❌ **Page templates** — Fixed structure
- ❌ **Images in analysis** — Text only
- ❌ **Page permissions** — Inherit from parent

## Criteria of Done

- [ ] All AC (1-10) verified
- [ ] Markdown to Notion blocks conversion accurate
- [ ] Page created as subpage of client
- [ ] All block types rendered correctly (heading, list, to-do, callout)
- [ ] Link returned to user
- [ ] Creation time <30s (measured)
- [ ] Tests: ≥75%
- [ ] Page title with date formatted correctly

## 🤖 CodeRabbit Integration

> **CodeRabbit Integration**: Disabled
>
> CodeRabbit CLI is not enabled em `core-config.yaml`.
> Validação de qualidade usará processo de revisão manual.
> Para ativar, defina `coderabbit_integration.enabled: true` no core-config.yaml

## Tasks / Subtasks

- [x] Task 1: Criar conversor Markdown → Notion Blocks (AC: 2, 3, 4, 5)
  - [x] 1.1 Criar `packages/integrations/src/notion/notion.helpers.ts`
  - [x] 1.2 Função `markdownToNotionBlocks(markdown: string): NotionBlock[]`
  - [x] 1.3 Mapeamentos obrigatórios:
    - `# Título` → `heading_1`
    - `## Subtítulo` → `heading_2`
    - `### Subsubtítulo` → `heading_3`
    - `- item` → `bulleted_list_item`
    - `- [ ] ação` → `to_do` com `checked: false`
    - `> texto` → `callout` com emoji `💡`
    - `---` → `divider`
    - Parágrafo normal → `paragraph`
    - `**negrito**` dentro de blocos → `bold: true` no rich_text
  - [x] 1.4 Detectar emoji em callouts: `> ⚠️ texto` → emoji do callout = `⚠️`
  - [x] 1.5 Função `buildRichText(text: string): RichTextItem[]` — parseia negrito, itálico, código inline
  - [x] 1.6 Criar tipo `NotionBlock` em `packages/integrations/src/notion/notion.types.ts` (wrapper tipado da API)

- [x] Task 2: Implementar `PlanOfAttackService` (AC: 1, 3, 6, 9, 10)
  - [x] 2.1 Criar `packages/core/src/clients/PlanOfAttackService.ts`
  - [x] 2.2 Método `createPlanPage(clientPageId: string, analysisMarkdown: string, clientName: string): Promise<string>` — retorna URL da página criada
  - [x] 2.3 Fluxo:
    1. Chamar `markdownToNotionBlocks(analysisMarkdown)` para converter análise
    2. Montar página com título `🎯 Plano de Ataque — ${date}`
    3. Chamar `notionClient.createPage(clientPageId, title, blocks)`
    4. Retornar URL: `https://notion.so/${pageId.replace(/-/g, '')}`
  - [x] 2.4 Método auxiliar `formatDate(): string` → `DD/MM/YYYY` em pt-BR

- [x] Task 3: Implementar `NotionClient.createPage()` (AC: 1, 9)
  - [x] 3.1 Adicionar método `createPage(parentPageId: string, title: string, blocks: NotionBlock[]): Promise<string>` em `NotionClient`
  - [x] 3.2 Chamar `this.client.pages.create()` com:
    ```typescript
    {
      parent: { page_id: parentPageId },   // subpágina do cliente
      properties: {
        title: { title: [{ text: { content: title } }] }
      },
      children: blocks,                      // máx 100 blocos por request
    }
    ```
  - [x] 3.3 Notion API tem limite de **100 blocos por request** — implementar paginação se análise gerar mais:
    - Criar página com primeiros 100 blocos
    - Chamar `blocks.children.append()` para os blocos restantes em lotes de 100
  - [x] 3.4 Retornar `pageId` da página criada
  - [x] 3.5 Em caso de erro: lançar `AppError('Failed to create Notion page', 'NOTION_001')`
  - [x] 3.6 Invalidar cache de clientes após criação (novo Plano aparece na listagem)

- [x] Task 4: Atualizar `IntentParser` para detectar "confirmar" (AC: 8)
  - [x] 4.1 Adicionar detecção de `PLAN_OF_ATTACK_CREATE` quando usuário confirma após preview
  - [x] 4.2 Trigger words para confirmação: "confirmar", "confirma", "sim", "pode criar", "cria no notion", "ok cria"
  - [x] 4.3 Distinguir de trigger "pronto" (Story 2.3): adicionar `entities.action: 'confirm' | 'generate'`
  - [x] 4.4 `action: 'generate'` → Stories 2.3 (gera análise)
  - [x] 4.5 `action: 'confirm'` → Story 2.4 (cria no Notion)

- [x] Task 5: Integrar no `ChatService` — handler de confirmação (AC: 6, 7, 8)
  - [x] 5.1 Atualizar `ChatService` para gerenciar estado pendente de confirmação
  - [x] 5.2 Adicionar `pendingAnalysis: string | null` no `SessionContext` (ou `ContextStore`)
  - [x] 5.3 Após streaming da Story 2.3: armazenar `analysisMarkdown` em `contextStore` para a sessão
  - [x] 5.4 Quando `intent === PLAN_OF_ATTACK_CREATE && entities.action === 'confirm'`:
    1. Recuperar `analysisMarkdown` da sessão
    2. Recuperar `activeClientId` e buscar `clientName`
    3. Chamar `planOfAttackService.createPlanPage(clientPageId, analysisMarkdown, clientName)`
    4. Retornar confirmação com link
    5. Limpar `pendingAnalysis` e `documentService.clearSession(sessionId)`
  - [x] 5.5 Quando "cancelar": limpar docs e `pendingAnalysis`, responder "Operação cancelada."

- [x] Task 6: Adicionar `pendingAnalysis` ao `ContextStore` (AC: 8)
  - [x] 6.1 Atualizar `packages/core/src/chat/ContextStore.ts`
  - [x] 6.2 Adicionar `Map<sessionId, string>` para `pendingAnalysis`
  - [x] 6.3 Métodos: `setPendingAnalysis(sessionId, markdown)`, `getPendingAnalysis(sessionId)`, `clearPendingAnalysis(sessionId)`

- [x] Task 7: Formatar confirmação e link (AC: 6, 7)
  - [x] 7.1 Link Notion: `https://notion.so/${pageId.replace(/-/g, '')}`
  - [x] 7.2 Mensagem Telegram (MarkdownV2 com escape):
    ```
    ✅ Plano de Ataque criado no Notion para *Empresa X*\!

    📄 [Abrir no Notion](https://notion.so/...)

    _Criado em DD/MM/YYYY às HH:mm_
    ```
  - [x] 7.3 Mensagem Web UI (Markdown simples):
    ```
    ✅ **Plano de Ataque criado no Notion para Empresa X!**

    📄 [Abrir no Notion](https://notion.so/...)

    *Criado em DD/MM/YYYY às HH:mm*
    ```

- [x] Task 8: Registrar Plano no histórico do cliente (AC: da Story 2.5 antecipado)
  - [x] 8.1 Após criação da página: chamar `notionClient.appendToHistory(clientPageId, historyEntry)` — stub se Story 2.5 não estiver pronta
  - [x] 8.2 `historyEntry`: `{ type: 'plano_de_ataque', pageId, date, documentCount }`
  - [x] 8.3 Se `appendToHistory` falhar: logar warning mas não falhar a operação principal

- [x] Task 9: Escrever testes (AC: 1, 2, 3, 4, 5)
  - [x] 9.1 Criar `packages/integrations/src/notion/__tests__/notion.helpers.test.ts`
    - Testar cada mapeamento: `#` → `heading_1`, `- [ ]` → `to_do`, `>` → `callout`, `---` → `divider`
    - Testar negrito dentro de parágrafo → `bold: true` em `rich_text`
    - Testar análise completa (string longa) → array de blocos na ordem correta
  - [x] 9.2 Criar `packages/core/src/clients/__tests__/PlanOfAttackService.test.ts`
    - Mock `NotionClient.createPage` → testar fluxo completo
    - Testar que `clearSession` é chamado após criação
    - Testar análise com >100 blocos → paginação chamada
  - [x] 9.3 Criar `packages/integrations/src/notion/__tests__/createPage.test.ts`
    - Mock `@notionhq/client` → testar criação com blocos, paginação
  - [x] 9.4 Atualizar `IntentParser.test.ts` — testar "confirmar", "sim", "ok cria" → `action: 'confirm'`

## Dev Notes

### Estrutura de Arquivos a Criar/Modificar

```
packages/integrations/src/notion/
├── notion.client.ts            # Adicionar createPage(), appendToHistory()
├── notion.helpers.ts           # NOVO — markdownToNotionBlocks()
├── notion.types.ts             # NOVO — NotionBlock types
└── __tests__/
    ├── notion.helpers.test.ts
    └── createPage.test.ts

packages/core/src/clients/
├── PlanOfAttackService.ts      # NOVO
└── __tests__/
    └── PlanOfAttackService.test.ts

packages/core/src/chat/
└── ContextStore.ts             # Adicionar pendingAnalysis Map
```

[Source: docs/architecture.md#Section-9-Unified-Project-Structure]

### Mapeamento Markdown → Notion Blocks

```typescript
// packages/integrations/src/notion/notion.helpers.ts

export function markdownToNotionBlocks(markdown: string): NotionBlock[] {
  const lines = markdown.split('\n');
  const blocks: NotionBlock[] = [];

  for (let i = 0; i < lines.length; i++) {
    const line = lines[i];

    // Divider
    if (line.trim() === '---') {
      blocks.push({ object: 'block', type: 'divider', divider: {} });
      continue;
    }

    // Headings
    if (line.startsWith('# ')) {
      blocks.push({
        object: 'block', type: 'heading_1',
        heading_1: { rich_text: buildRichText(line.slice(2)) }
      });
      continue;
    }
    if (line.startsWith('## ')) {
      blocks.push({
        object: 'block', type: 'heading_2',
        heading_2: { rich_text: buildRichText(line.slice(3)) }
      });
      continue;
    }
    if (line.startsWith('### ')) {
      blocks.push({
        object: 'block', type: 'heading_3',
        heading_3: { rich_text: buildRichText(line.slice(4)) }
      });
      continue;
    }

    // To-do (checklist)
    if (line.match(/^- \[ \] /)) {
      blocks.push({
        object: 'block', type: 'to_do',
        to_do: {
          rich_text: buildRichText(line.slice(6)),
          checked: false
        }
      });
      continue;
    }

    // Bulleted list
    if (line.startsWith('- ')) {
      blocks.push({
        object: 'block', type: 'bulleted_list_item',
        bulleted_list_item: { rich_text: buildRichText(line.slice(2)) }
      });
      continue;
    }

    // Callout (detecta emoji inicial)
    if (line.startsWith('> ')) {
      const content = line.slice(2);
      const emojiMatch = content.match(/^([\u{1F000}-\u{1FFFF}]|[⚠️💡🎯✅❌🔴🟡🟢])\s/u);
      const emoji = emojiMatch ? emojiMatch[1] : '💡';
      const text = emojiMatch ? content.slice(emojiMatch[0].length) : content;
      blocks.push({
        object: 'block', type: 'callout',
        callout: {
          rich_text: buildRichText(text),
          icon: { type: 'emoji', emoji }
        }
      });
      continue;
    }

    // Paragraph (ignora linhas vazias)
    if (line.trim()) {
      blocks.push({
        object: 'block', type: 'paragraph',
        paragraph: { rich_text: buildRichText(line) }
      });
    }
  }

  return blocks;
}

export function buildRichText(text: string): RichTextItem[] {
  // Parseia **negrito** e *itálico*
  const parts: RichTextItem[] = [];
  const regex = /(\*\*(.+?)\*\*|\*(.+?)\*|`(.+?)`|(.+?)(?=\*\*|\*|`|$))/g;
  let match;

  while ((match = regex.exec(text)) !== null) {
    if (!match[0]) break;
    if (match[2]) {
      parts.push({ type: 'text', text: { content: match[2] }, annotations: { bold: true } });
    } else if (match[3]) {
      parts.push({ type: 'text', text: { content: match[3] }, annotations: { italic: true } });
    } else if (match[4]) {
      parts.push({ type: 'text', text: { content: match[4] }, annotations: { code: true } });
    } else if (match[5]) {
      parts.push({ type: 'text', text: { content: match[5] } });
    }
  }

  return parts.length > 0 ? parts : [{ type: 'text', text: { content: text } }];
}
```

[Source: docs/architecture.md#Section-6.1-Core-Workflows | docs/prd.md#Section-2-AC-Story-2.4]

### NotionClient.createPage() — com Paginação

```typescript
// packages/integrations/src/notion/notion.client.ts (adição)

async createPage(
  parentPageId: string,
  title: string,
  blocks: NotionBlock[]
): Promise<string> {
  const BATCH_SIZE = 100;
  const firstBatch = blocks.slice(0, BATCH_SIZE);
  const remaining = blocks.slice(BATCH_SIZE);

  // Criar página com primeiros 100 blocos
  const page = await this.client.pages.create({
    parent: { page_id: parentPageId },
    properties: {
      title: { title: [{ text: { content: title } }] }
    },
    children: firstBatch as any,
  });

  // Adicionar blocos restantes em lotes
  if (remaining.length > 0) {
    for (let i = 0; i < remaining.length; i += BATCH_SIZE) {
      const batch = remaining.slice(i, i + BATCH_SIZE);
      await this.client.blocks.children.append({
        block_id: page.id,
        children: batch as any,
      });
    }
  }

  return page.id;
}
```

[Source: docs/architecture.md#Section-5.3-Endpoints | Notion API limit: 100 blocks]

### Estrutura Esperada da Página Notion

```
📄 🎯 Plano de Ataque — 20/02/2026
│
├── heading_1: "📋 Contexto Geral"
├── paragraph: "[Nome do cliente] | Gerado por ARIA em 20/02/2026"
├── divider
│
├── heading_1: "📊 Resumo por Setor"
├── heading_2: "Setor Comercial"
├── bulleted_list_item: "Ponto chave 1"
├── bulleted_list_item: "Ponto chave 2"
├── callout (💡): "Insight crítico"
├── divider
│
├── heading_2: "Setor de Marketing"
├── bulleted_list_item: "Estratégia 1"
├── divider
│
├── heading_1: "🔗 Análise Integrada"
├── paragraph: "[Síntese estratégica]"
├── callout (🎯): "Recomendação principal"
├── divider
│
├── heading_1: "✅ Checklist de Ações"
├── heading_2: "🔴 Alta Prioridade"
├── to_do: "Ação 1" (checked: false)
├── to_do: "Ação 2" (checked: false)
├── heading_2: "🟡 Média Prioridade"
└── to_do: "Ação 3" (checked: false)
```

[Source: docs/prd.md#Section-6-Epic-2-Story-2.4-AC]

### Gestão de Estado de Confirmação

```typescript
// Fluxo de estados no ContextStore:
//
// 1. Story 2.3: após análise gerada
//    contextStore.setPendingAnalysis(sessionId, markdownText)
//
// 2. Story 2.4: após usuário confirmar
//    const markdown = contextStore.getPendingAnalysis(sessionId)
//    → criar página Notion
//    contextStore.clearPendingAnalysis(sessionId)
//    documentService.clearSession(sessionId)
//
// 3. Usuário cancelar:
//    contextStore.clearPendingAnalysis(sessionId)
//    documentService.clearSession(sessionId)
//    → "Operação cancelada."
```

### KPI MVP — Objetivo < 3 Minutos Total (AC: 10)

Do PRD: "Plano de Ataque criado no Notion em menos de 3 minutos a partir de 2 documentos enviados"

Estimativa de tempo por etapa:
| Etapa | Tempo estimado |
|-------|---------------|
| Upload + parse 2 PDFs (Story 2.2) | ~5-10s |
| Análise Claude (Story 2.3) | ~15-30s (streaming) |
| Usuário lê preview + confirma | ~30-60s |
| Criação da página Notion (Story 2.4) | ~3-8s |
| **Total** | **~53-108s < 3min** ✅ |

### Dependência de Stories Anteriores

- **Story 2.1:** `NotionClient` com autenticação e `ClientProfile` — adicionar `createPage()` e `appendToHistory()`
- **Story 2.2:** `DocumentService.clearSession()` — limpar docs após confirmação
- **Story 2.3:** `analysisMarkdown` gerado e armazenado em `pendingAnalysis` — consumido aqui
- **Story 1.5:** `ContextStore` com `setActiveClient()` — estender com `pendingAnalysis`

### Testing

**Framework:** Vitest

**Casos críticos de `notion.helpers.test.ts`:**
```typescript
describe('markdownToNotionBlocks', () => {
  it('converte # em heading_1', () => {
    const blocks = markdownToNotionBlocks('# Título Principal');
    expect(blocks[0].type).toBe('heading_1');
    expect(blocks[0].heading_1.rich_text[0].text.content).toBe('Título Principal');
  });

  it('converte - [ ] em to_do com checked false', () => {
    const blocks = markdownToNotionBlocks('- [ ] Ação importante');
    expect(blocks[0].type).toBe('to_do');
    expect(blocks[0].to_do.checked).toBe(false);
  });

  it('converte > em callout com emoji 💡', () => {
    const blocks = markdownToNotionBlocks('> Insight crítico aqui');
    expect(blocks[0].type).toBe('callout');
    expect(blocks[0].callout.icon.emoji).toBe('💡');
  });

  it('converte --- em divider', () => {
    const blocks = markdownToNotionBlocks('---');
    expect(blocks[0].type).toBe('divider');
  });

  it('análise completa gera blocos na ordem correta', () => {
    const markdown = '# Título\n- item\n- [ ] tarefa\n> insight\n---';
    const blocks = markdownToNotionBlocks(markdown);
    expect(blocks.map(b => b.type)).toEqual([
      'heading_1', 'bulleted_list_item', 'to_do', 'callout', 'divider'
    ]);
  });
});
```

[Source: docs/architecture.md#Section-13-Testing-Strategy]

## Change Log

| Data | Versão | Descrição | Autor |
|------|--------|-----------|-------|
| 2026-02-20 | 1.0 | Story criada | River (SM) |

## Dev Agent Record

### Agent Model Used
_A ser preenchido pelo @dev_

### Debug Log References
_A ser preenchido pelo @dev_

### Completion Notes List
_A ser preenchido pelo @dev_

### File List
_A ser preenchido pelo @dev_

## QA Results
_A ser preenchido pelo @qa_
