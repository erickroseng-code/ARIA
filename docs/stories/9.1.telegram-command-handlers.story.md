---
executor: "@dev"
quality_gate: "@dev"
quality_gate_tools: [auth_validation, command_handler_review]
epic: 9
story: 9.1
status: Ready for Dev
---

# Story 9.1: Telegram — Webhook e Command Handlers

## Status
Ready for Dev

## Story

**As a** operador da Aria,
**I want** enviar comandos ao bot Telegram e receber respostas em tempo real,
**so that** eu possa consultar o status da Aria, pedir relatórios e controlar o Atlas diretamente pelo celular.

## Acceptance Criteria

1. **Webhook endpoint:** `POST /api/telegram/webhook` processa updates do Telegram (mensagens e comandos). Responde HTTP 200 sempre (Telegram exige isso para não reenviar).
2. **Segurança:** Todas as mensagens recebidas verificam se `message.chat.id` está em `ALLOWED_TELEGRAM_IDS` (env var já existe, comma-separated). Mensagens de IDs não autorizados são ignoradas silenciosamente.
3. **Comando `/status`:** Responde com status geral da Aria:
   ```
   🤖 Aria — Status
   ✅ API: online
   🔗 Google: conectado
   📊 Atlas: ATLAS_WRITE_ENABLED=false (modo seguro)
   🔔 Telegram: ativo
   ⏰ Uptime: Xh Ym
   ```
4. **Comando `/atlas`:** Responde com resumo das últimas entradas do audit log (até 5 ações recentes de `atlas_audit_log`).
5. **Comando `/help`:** Lista comandos disponíveis com descrição breve.
6. **Comando desconhecido:** Responde "Comando não reconhecido. Use /help para ver os comandos disponíveis."
7. **Função utilitária `sendTelegram(chatId, text)`:** Reutilizada pelo resto do sistema (já existe lógica similar em `atlas-notifier.ts` — consolidar).

## Scope

**IN:**
- Novo arquivo `aria/apps/api/src/routes/telegram-webhook.routes.ts`
- Novo arquivo `aria/apps/api/src/shared/telegram.ts` — utilitário centralizado `sendTelegram()`
- Registrar rota no `server.ts` com prefix `/api/telegram`
- Refatorar `atlas-notifier.ts` para usar `sendTelegram()` de `shared/telegram.ts`

**OUT:**
- Não registrar webhook automaticamente na API do Telegram (isso é manual/parte do deploy no Render)
- Não implementar `/report` nesta story (Story 9.2)
- Não alterar bot Telegram existente se houver polling ativo (verificar)

## Dependencies

- Nenhuma story prévia obrigatória — TELEGRAM_BOT_TOKEN e ALLOWED_TELEGRAM_IDS já estão no .env

## Complexity Estimate

**T-Shirt:** Small (S)
**Story Points:** 3
**Dev Time:** ~3 horas

## Business Value

Controle da Aria pelo celular sem abrir nenhum painel. O operador pode checar o status e ver o que o Atlas fez enquanto estiver fora.

## Risks

- **Risco:** Telegram pode reenviar updates se o webhook demorar > 5s para responder
- **Mitigação:** Responder HTTP 200 imediatamente, processar o comando de forma assíncrona (sem await no handler principal)

## Tasks / Subtasks

- [ ] **Task 1** — Criar `shared/telegram.ts` com `sendTelegram()` centralizado (AC: 7)
  ```ts
  // aria/apps/api/src/shared/telegram.ts
  export async function sendTelegram(chatId: string | number, text: string): Promise<void> {
    const botToken = process.env.TELEGRAM_BOT_TOKEN;
    if (!botToken) return;
    try {
      await fetch(`https://api.telegram.org/bot${botToken}/sendMessage`, {
        method: 'POST',
        headers: { 'Content-Type': 'application/json' },
        body: JSON.stringify({ chat_id: chatId, text, parse_mode: 'HTML' }),
      });
    } catch (err) {
      console.error('[Telegram] Falha ao enviar mensagem:', err);
    }
  }
  ```
  - **Nota:** Usar `parse_mode: 'HTML'` ao invés de MarkdownV2 para simplicidade (menos escaping)

- [ ] **Task 2** — Refatorar `atlas-notifier.ts` para usar `sendTelegram()` (AC: 7)
  - Remover o `fetch` inline e usar `sendTelegram(chatId, text)` de `shared/telegram.ts`
  - Simplificar a formatação (remover escaping de MarkdownV2 → usar HTML tags `<b>`, `<i>`)

- [ ] **Task 3** — Criar `telegram-webhook.routes.ts` (AC: 1, 2, 3, 4, 5, 6)
  ```ts
  // aria/apps/api/src/routes/telegram-webhook.routes.ts
  import type { FastifyInstance } from 'fastify';
  import { sendTelegram } from '../shared/telegram';
  import { getAuditLogs } from '../modules/traffic/agents/atlas-audit';
  import { isAtlasWriteEnabled } from '../modules/traffic/agents/atlas-orchestrator';
  import { isWorkspaceConfigured } from '@aria/integrations';

  function isAuthorized(chatId: number): boolean {
    const allowed = (process.env.ALLOWED_TELEGRAM_IDS ?? '')
      .split(',').map(id => id.trim()).filter(Boolean);
    return allowed.includes(String(chatId));
  }

  export async function registerTelegramWebhookRoutes(fastify: FastifyInstance): Promise<void> {
    fastify.post('/webhook', async (req, reply) => {
      // Always respond 200 immediately (Telegram requirement)
      reply.status(200).send({ ok: true });

      const body = req.body as any;
      const message = body?.message;
      if (!message?.text || !message?.chat?.id) return;

      const chatId = message.chat.id;
      if (!isAuthorized(chatId)) return;

      const text = message.text.trim();
      const command = text.split(' ')[0].toLowerCase();

      // Handle commands asynchronously (already replied 200)
      setImmediate(async () => {
        switch (command) {
          case '/status':
            await handleStatus(chatId);
            break;
          case '/atlas':
            await handleAtlas(chatId);
            break;
          case '/help':
            await handleHelp(chatId);
            break;
          default:
            await sendTelegram(chatId, 'Comando não reconhecido. Use /help para ver os comandos disponíveis.');
        }
      });
    });
  }

  async function handleStatus(chatId: number): Promise<void> {
    const googleOk = await isWorkspaceConfigured().catch(() => false);
    const atlasMode = isAtlasWriteEnabled() ? '⚡ ativo' : '🔒 modo seguro';
    const uptimeSec = Math.floor(process.uptime());
    const h = Math.floor(uptimeSec / 3600);
    const m = Math.floor((uptimeSec % 3600) / 60);

    await sendTelegram(chatId,
      `🤖 <b>Aria — Status</b>\n` +
      `✅ API: online\n` +
      `${googleOk ? '✅' : '❌'} Google: ${googleOk ? 'conectado' : 'desconectado'}\n` +
      `📊 Atlas: ${atlasMode}\n` +
      `🔔 Telegram: ativo\n` +
      `⏰ Uptime: ${h}h ${m}m`
    );
  }

  async function handleAtlas(chatId: number): Promise<void> {
    const logs = getAuditLogs(undefined, 5);
    if (logs.length === 0) {
      await sendTelegram(chatId, '📊 <b>Atlas</b>\nNenhuma ação registrada ainda.');
      return;
    }
    const lines = ['📊 <b>Atlas — Últimas ações</b>'];
    for (const log of logs) {
      const icon = log.result.startsWith('ERROR') ? '❌' : '✅';
      const mode = (log.dry_run as unknown as number) === 1 ? ' [dry-run]' : '';
      lines.push(`${icon} ${log.action} (${log.entity_id})${mode}: ${log.result}`);
    }
    await sendTelegram(chatId, lines.join('\n'));
  }

  async function handleHelp(chatId: number): Promise<void> {
    await sendTelegram(chatId,
      `🤖 <b>Aria — Comandos</b>\n\n` +
      `/status — Status geral da Aria\n` +
      `/atlas — Últimas ações autônomas do Atlas\n` +
      `/help — Esta mensagem`
    );
  }
  ```

- [ ] **Task 4** — Registrar rota em `server.ts` (AC: 1)
  ```ts
  import { registerTelegramWebhookRoutes } from './routes/telegram-webhook.routes';
  // ...
  await fastify.register(registerTelegramWebhookRoutes, { prefix: '/api/telegram' });
  ```

- [ ] **Task 5** — Testes (AC: 1, 2)
  - Teste: chat_id não autorizado → sem resposta (ignora silenciosamente)
  - Teste: `/help` → retorna lista de comandos
  - Teste: comando desconhecido → mensagem de fallback

## Dev Notes

### Como registrar o webhook no Telegram (manual — feito após deploy no Render)

```bash
curl -X POST "https://api.telegram.org/bot{TOKEN}/setWebhook" \
  -d "url=https://sua-aria.onrender.com/api/telegram/webhook"
```

Isso será feito na Story 11 (Deploy no Render).

### Localização dos arquivos

```
aria/apps/api/src/
├── shared/
│   └── telegram.ts              ← CRIAR (utilitário centralizado)
├── routes/
│   └── telegram-webhook.routes.ts ← CRIAR
└── server.ts                    ← MODIFICAR (registrar nova rota)

aria/apps/api/src/modules/traffic/agents/
└── atlas-notifier.ts            ← MODIFICAR (usar sendTelegram de shared/)
```

## Definition of Done

- [ ] `POST /api/telegram/webhook` processa comandos `/status`, `/atlas`, `/help`
- [ ] IDs não autorizados ignorados
- [ ] `sendTelegram()` centralizado em `shared/telegram.ts`
- [ ] `atlas-notifier.ts` refatorado para usar utilitário central
- [ ] Testes passando
- [ ] `npm run lint` e `npm run typecheck` sem erros novos

## Change Log

| Data | Agente | Mudança |
|------|--------|---------|
| 2026-03-10 | @sm (River) | Story criada — Epic 9, Telegram + GitHub Actions |
